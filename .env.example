# URL of the deployed playit Drive proxy Worker (see worker/README.md).
# Required for Google Drive playback. Copy to .env.local for local development.
VITE_DRIVE_PROXY=https://playit-proxy.your-subdomain.workers.dev
